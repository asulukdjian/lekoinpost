<div class="message-container <%= message.user %>" id="message-<%= message.id %>">
  <i class="author">
    <span><%= message.user.full_name %></span>
    <small><%= message.created_at.strftime("%a %b %e at %l:%M%p") %></small>
  </i>
  <p><%= message.content %></p>
</div>
